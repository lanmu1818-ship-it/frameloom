# 贡献指南

请先按 README 安装和运行。修改应限于浏览器前端及公开数据契约，不要引入服务端配置或商业环境默认地址。

提交前运行 `pnpm check:public`、`pnpm test`、`pnpm typecheck` 和 `pnpm build`。新增配置同步更新 `.env.example` 与文档；修复用户行为时补充有意义的回归验证。PR 请说明问题、变更效果、验证结果及界面截图（如适用）。

请不要在 Issue、截图或 PR 中提交访问令牌、Cookie、用户数据或内部链接。提交贡献表示你有权按本项目 Apache-2.0 许可提供该贡献；第三方代码和素材应保留来源与许可。
